export default interface UiTheme {
    dark: {
        bg: string
        color: string
        deskBg?: string,
        productColor?: string,
    }
    light: {
        bg: string
        color: string,
        deskBg?: string,
        productColor?: string,
    }
}